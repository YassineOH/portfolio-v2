---
import type { HTMLAttributes } from 'astro/types';

interface Props extends HTMLAttributes<'article'> {}
const { class: className, ...attrs } = Astro.props;
---

<article
  class:list={['container mx-auto max-w-80 lg:max-w-[560px]', className]}
  {...attrs}
>
  <slot />
</article>
